export class AdModel{
  constructor(adData){
    this.id = adData.id
    this.title = adData.title
    this.linkURL = adData.linkURL
    this.tall = adData.tall
    this.square = adData.square
    this.banner = adData.banner
  }
}